
# URL de conexión a la base de datos (SQLite en este caso)
DATABASE_URL="file:./dev.db"

# Clave secreta para firmar los JSON Web Tokens (JWT)
# Puedes generar una con el comando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="tu_jwt_secret_aqui"

# Clave de API para el servicio de Google Generative AI
GOOGLE_API_KEY="tu_google_api_key_aqui"
